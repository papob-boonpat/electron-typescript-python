{
  "name": "typescript",
  "version": "1.0.0",
  "main": "./app/build/main.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "yarn run build && electron .",
    "build-osx": "electron-packager .  --out ./dist --platform darwin --overwrite --ignore='python|app/src'"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "electron": "^16.0.5",
    "electron-packager": "^15.4.0",
    "typescript": "^4.5.4"
  }
}
